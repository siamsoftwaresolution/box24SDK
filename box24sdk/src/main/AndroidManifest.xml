<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="box24.com.box24sdk">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application>
        <activity
            android:name=".activity.ActivityBox24Menu"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ActivityBox24Drop"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.ActivityBox24DropQuickPin"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.ActivityBox24Locker"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.ActivityBox24Pic"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.ActivityBox24Introduce"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.ActivityBox24Location"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.ActivityLocation"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.ActivityLocationDetail"
            android:screenOrientation="portrait" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->



    </application>

</manifest>